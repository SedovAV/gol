CC = gcc
CFLAGS = -Wall -Wextra -pedantic -std=c99
LDFLAGS =
INCLUDES = -Isrc

SRC_DIR = src
BUILD_DIR = build
TEST_NAME = Quest_2

SRC = $(SRC_DIR)/list.c $(SRC_DIR)/list_test.c
OBJ = $(SRC:.c=.o)
HEADERS = $(SRC_DIR)/list.h

all: $(TEST_NAME)

$(TEST_NAME): $(OBJ)
	$(CC) $(CFLAGS) $(LDFLAGS) -o $@ $^

$(OBJ): $(HEADERS)

clean:
	rm -f $(BUILD_DIR)/*

.PHONY: all clean
